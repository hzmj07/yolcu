import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
  TextInput
} from 'react-native';



const Setting =() => {

  return<Text> Setting </Text>
}

const styles = StyleSheet.create({

});

export default Setting;
